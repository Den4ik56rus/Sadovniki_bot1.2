# Анализ готовности бота к первому запуску

**Дата:** 2026-02-15
**Версия:** 1.2.2
**Статус:** Предзапусковая проверка

---

## 📋 Executive Summary

**Основные системы:**
- ✅ Режим консультаций: **ГОТОВ**
- ⚠️ Реферальная программа: **ЧАСТИЧНО ГОТОВ** (требует доработки интеграции)
- ✅ Прием платежей (YooKassa): **ГОТОВ** (тестовый режим)

**Критичные пропуски:**
1. ❌ Реферальная система не интегрирована в регистрацию пользователей
2. ⚠️ Отсутствует система триальных вопросов для новых пользователей
3. ⚠️ Ngrok URL в .env нестабилен (меняется при перезапуске)
4. ⚠️ 6 миграций БД не применены (schema 37-42)

**Рекомендация:** Необходима доработка реферальной системы и настройка триальных вопросов перед запуском.

---

## 🎯 Критический функционал для запуска

### 1. ✅ Режим консультаций

**Статус:** Полностью функционален

**Реализовано:**
- ✅ Определение 12 типов культур (клубника, малина, ежевика, смородина, голубика, жимолость, крыжовник, ирга, арония)
- ✅ 7 категорий консультаций (питание, посадка, защита, почва, сорта, обрезка, другое)
- ✅ Многоэтапные диалоги с уточнениями (летняя/ремонтантная)
- ✅ RAG-поиск в базе знаний (3-уровневая система: Q&A → приоритетные доки → общие доки)
- ✅ Культуроспецифичные промпты для клубники/малины (питание)
- ✅ Markdown форматирование ответов
- ✅ Логирование всех консультаций
- ✅ Real-time мониторинг в Admin Panel (SSE)

**Файлы:**
- [src/handlers/consultation/entry.py](src/handlers/consultation/entry.py:681) — основной обработчик
- [src/services/llm/consultation_llm.py](src/services/llm/consultation_llm.py) — генерация ответов
- [src/services/rag/unified_retriever.py](src/services/rag/unified_retriever.py:681) — поиск в KB
- [docs/features/CONSULTATION_FLOW.md](docs/features/CONSULTATION_FLOW.md) — документация

**Что протестировать:**
```
1. Отправить вопрос: "Как подкормить клубнику?"
   → Должен спросить: "Летняя или ремонтантная?"
   → После ответа: детальная консультация с использованием RAG

2. Отправить вопрос: "Когда обрезать малину ремонтантную?"
   → Должен сразу дать ответ (культура определена точно)

3. Отправить неясный вопрос: "Как правильно ухаживать?"
   → Должен задать уточняющий вопрос: "О какой культуре речь?"

4. Проверить логирование в Admin Panel → Live Feed
   → Консультация должна появиться в реальном времени
```

---

### 2. ⚠️ Реферальная программа

**Статус:** База данных готова, но интеграция не завершена

**Реализовано:**
- ✅ Схема БД (schema_46_referrals.sql)
- ✅ Репозиторий функций (referral_repo.py)
- ✅ Генерация уникальных реферальных кодов (8 символов)
- ✅ Начисление бонусов (3 вопроса рефереру, 2 вопроса рефери)
- ✅ Статистика рефералов (get_referral_stats)

**❌ НЕ РЕАЛИЗОВАНО:**
1. **Отображение реферального кода в профиле пользователя**
   - Файл: [src/handlers/menu/profile.py](src/handlers/menu/profile.py)
   - Нужно добавить блок с реферальным кодом и ссылкой
   - Пример:
     ```
     🔗 Реферальная программа
     Ваш код: ABC12345
     Ссылка: https://t.me/garden_bot_ai_bot?start=ref_ABC12345

     Приглашено друзей: 5
     Вы получили: +15 вопросов
     ```

2. **Обработка deeplink при старте бота с реферальным кодом**
   - Файл: [src/handlers/menu/start.py](src/handlers/menu/start.py)
   - Текущий код НЕ обрабатывает `/start ref_ABC12345`
   - Нужно:
     - Парсить аргумент команды `/start`
     - Извлекать реферальный код (после `ref_`)
     - Проверять существование кода в БД
     - Создавать связь referrer-referee
     - Начислять бонусы ПОСЛЕ первой оплаты

3. **Начисление бонусов после первой оплаты**
   - Файл: [src/services/payments/payment_service.py](src/services/payments/payment_service.py)
   - Функция `process_payment_success()` НЕ вызывает `grant_referral_bonuses()`
   - Нужно добавить проверку:
     - Это первый платёж пользователя?
     - Есть ли реферальная связь?
     - Если да → начислить бонусы обоим

4. **Отображение реферальной информации в Admin Panel CRM**
   - Файл: [admin-webapp/src/components/crm/LeftPanel/MainTab.tsx](admin-webapp/src/components/crm/LeftPanel/MainTab.tsx)
   - Есть упоминания referral, но нет полной интеграции
   - Нужно показывать:
     - Кто пригласил клиента
     - Кого клиент пригласил
     - Начисленные бонусы

**Файлы для доработки:**
```
1. src/handlers/menu/start.py
   - Добавить обработку deeplink ref_CODE
   - Создать реферальную связь при первом запуске

2. src/handlers/menu/profile.py
   - Показать реферальный код и статистику
   - Кнопка "Пригласить друга" с share-ссылкой

3. src/services/payments/payment_service.py
   - В process_payment_success() добавить:
     * Проверка первого платежа
     * Вызов grant_referral_bonuses()

4. admin-webapp/src/components/crm/LeftPanel/MainTab.tsx
   - Секция "Реферальная информация"
```

**Оценка работ:** 3-4 часа доработки

---

### 3. ✅ Прием платежей (YooKassa)

**Статус:** Функционал реализован, работает в тестовом режиме

**Реализовано:**
- ✅ Интеграция с YooKassa API (shop_id + secret_key в .env)
- ✅ Создание платежей (подписки + пакеты токенов)
- ✅ Обработка webhook уведомлений
- ✅ Автоматическая активация подписки после оплаты
- ✅ Начисление токенов (разовые пакеты)
- ✅ Обработка отмены платежей
- ✅ Логирование ошибок платежей
- ✅ Отображение платежей в Admin Panel

**Конфигурация (.env):**
```bash
YOOKASSA_SHOP_ID=1233746
YOOKASSA_SECRET_KEY=test_MlN3GUKbwI0IpyUM9hKFt3_tzKi2OOUSJKQQz6KgXBA
YOOKASSA_TEST_MODE=true  ⚠️ ТЕСТОВЫЙ РЕЖИМ
YOOKASSA_RETURN_URL=https://t.me/garden_bot_ai_bot
YOOKASSA_WEBHOOK_URL=https://2a188cab24b8.ngrok-free.app/api/webhooks/yookassa  ⚠️ NGROK
YOOKASSA_SEND_RECEIPT=true
YOOKASSA_TAX_SYSTEM_CODE=1
```

**⚠️ КРИТИЧНЫЕ МОМЕНТЫ:**

1. **Ngrok URL нестабилен**
   - Текущий URL: `https://2a188cab24b8.ngrok-free.app`
   - Меняется при каждом перезапуске ngrok
   - **Решение:**
     - Для production: использовать статический домен (ngrok paid plan ИЛИ VPS с белым IP)
     - Для тестирования: обновлять URL в YooKassa dashboard после каждого перезапуска
     - Альтернатива: localtunnel, serveo, или localhost.run (бесплатные аналоги)

2. **Тестовый режим включен**
   - `YOOKASSA_TEST_MODE=true` → реальные деньги НЕ принимаются
   - Для production: изменить на `false` и получить production secret_key от YooKassa
   - Тестовые карты: https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing

3. **Webhook требует публичного доступа**
   - Backend должен быть доступен по HTTPS
   - Ngrok — временное решение для разработки
   - Production: нужен реальный домен с SSL сертификатом

**Файлы:**
- [src/services/payments/payment_service.py](src/services/payments/payment_service.py) — создание платежей
- [src/api/handlers/webhooks.py](src/api/handlers/webhooks.py:114) — обработка уведомлений
- [src/handlers/payments/subscription.py](src/handlers/payments/subscription.py) — покупка подписок
- [src/handlers/payments/tokens.py](src/handlers/payments/tokens.py) — покупка токенов
- [db/schema_30_payments.sql](db/schema_30_payments.sql) — схема БД

**Что протестировать:**
```
1. Меню оплаты: /start → "💰 Купить вопросы"
   → Должны отображаться планы подписок и пакеты токенов

2. Покупка подписки:
   - Выбрать план → получить ссылку на оплату YooKassa
   - Оплатить тестовой картой (4111 1111 1111 1111, 12/24, 123)
   - Webhook должен активировать подписку
   - Проверить: user.subscription_status = 'active'

3. Покупка токенов:
   - Выбрать пакет → оплатить
   - Проверить: user.tokens увеличился на нужное количество

4. Отмена платежа:
   - Создать платеж → не оплачивать → отменить
   - Webhook должен обработать отмену
   - Проверить: payment.status = 'canceled'

5. Admin Panel:
   - Открыть CRM → выбрать клиента
   - Вкладка "Платежи" → должны отображаться транзакции
   - Activity Feed → события платежей (pending → succeeded)
```

---

## 🔧 Миграции БД

**Статус:** 6 миграций не применены

**Применено (до schema_36):**
- ✅ schema_01-32: базовые таблицы, prompt system v1, payments
- ✅ schema_33-36: культуроспецифичные промпты (малина, смородина, голубика)

**❌ НЕ ПРИМЕНЕНО (schema_37-42 + 44-46):**

1. **schema_37_documents_full_text.sql** — full-text search для документов
2. **schema_38_embedding_nullable.sql** — nullable embeddings для документов без векторизации
3. **schema_39_admin_settings.sql** — таблица настроек админки
4. **schema_40_culture_rules_split.sql** — разделение правил по культурам
5. **schema_41_category_prompts_migration.sql** — миграция категорийных промптов
6. **schema_42_llm_settings.sql** — настройки LLM моделей в БД
7. **schema_44_trial_questions.sql** — система триальных вопросов
8. **schema_45_pricing_update.sql** — обновление цен
9. **schema_46_referrals.sql** — реферальная программа ✅ (ДОЛЖНА БЫТЬ ПРИМЕНЕНА)

**⚠️ КРИТИЧНО:**
- **schema_44_trial_questions.sql** — без этого новые пользователи НЕ получат пробные вопросы!
- **schema_46_referrals.sql** — без этого реферальная программа НЕ РАБОТАЕТ!

**Команды для применения:**
```bash
cd /Users/denis/Desktop/Main/Sadovniki-bot/Sadovniki_bot1.2

psql -h localhost -U bot_user -d garden_bot -f db/schema_37_documents_full_text.sql
psql -h localhost -U bot_user -d garden_bot -f db/schema_38_embedding_nullable.sql
psql -h localhost -U bot_user -d garden_bot -f db/schema_39_admin_settings.sql
psql -h localhost -U bot_user -d garden_bot -f db/schema_40_culture_rules_split.sql
psql -h localhost -U bot_user -d garden_bot -f db/schema_41_category_prompts_migration.sql
psql -h localhost -U bot_user -d garden_bot -f db/schema_42_llm_settings.sql
psql -h localhost -U bot_user -d garden_bot -f db/schema_44_trial_questions.sql
psql -h localhost -U bot_user -d garden_bot -f db/schema_45_pricing_update.sql
psql -h localhost -U bot_user -d garden_bot -f db/schema_46_referrals.sql
```

**Верификация:**
```sql
-- Проверить версию схемы
SELECT MAX(version) FROM schema_migrations;
-- Должно быть >= 46

-- Проверить таблицу реферальной программы
\d referrals
-- Должна существовать

-- Проверить триальные вопросы
SELECT * FROM admin_settings WHERE key = 'trial_questions_count';
-- Должна быть строка с лимитом пробных вопросов
```

---

## 🧪 План тестирования перед запуском

### Подготовка

1. **Применить все миграции** (см. раздел выше)
2. **Запустить backend:** `python -m src`
3. **Запустить ngrok (для webhook):** `ngrok http 8080`
4. **Обновить YOOKASSA_WEBHOOK_URL в .env** с новым ngrok URL
5. **Перезапустить backend** (чтобы применить новый URL)

---

### Тест 1: Новый пользователь + Триальные вопросы

**Цель:** Проверить, что новый пользователь получает пробные вопросы

**Шаги:**
1. Удалить существующего тестового пользователя из БД (если есть):
   ```sql
   DELETE FROM users WHERE telegram_user_id = 123456789;
   ```
2. Отправить `/start` от нового Telegram аккаунта
3. Проверить создание пользователя в БД:
   ```sql
   SELECT id, telegram_user_id, tokens, trial_questions_granted
   FROM users
   WHERE telegram_user_id = 123456789;
   ```
4. **Ожидаемый результат:**
   - `tokens` = 3 (или значение из admin_settings.trial_questions_count)
   - `trial_questions_granted` = true
5. Отправить консультационный вопрос
6. Проверить, что токены уменьшились: `tokens` = 2

**Критерий успеха:** ✅ Новый пользователь получил триальные вопросы автоматически

---

### Тест 2: Реферальная программа

**Цель:** Проверить полный цикл реферальной программы

**⚠️ ТРЕБУЕТ ДОРАБОТКИ КОДА (см. раздел "Реферальная программа")**

**Шаги (после доработки):**

1. **Пользователь A (реферер):**
   - Отправить `/start`
   - Открыть профиль → получить реферальный код (например, `ABC12345`)
   - Скопировать реферальную ссылку: `https://t.me/garden_bot_ai_bot?start=ref_ABC12345`

2. **Пользователь B (реферал):**
   - Перейти по реферальной ссылке
   - Бот должен написать: "Вас пригласил [имя пользователя A]! Получите +2 вопроса после первой оплаты."
   - Проверить БД:
     ```sql
     SELECT * FROM referrals WHERE referee_id = (SELECT id FROM users WHERE telegram_user_id = [B_tg_id]);
     ```
   - `referrer_id` должен указывать на пользователя A
   - `referrer_bonus_granted` = false, `referee_bonus_granted` = false

3. **Пользователь B совершает первую оплату:**
   - Купить пакет токенов / подписку
   - Оплатить тестовой картой
   - Webhook обрабатывает платеж → начисляет бонусы

4. **Проверить бонусы:**
   ```sql
   -- Проверить tokens пользователей
   SELECT telegram_user_id, tokens FROM users WHERE id IN ([A_id], [B_id]);

   -- Проверить флаги начисления
   SELECT referrer_bonus_granted, referee_bonus_granted
   FROM referrals
   WHERE referrer_id = [A_id] AND referee_id = [B_id];
   ```
   - Пользователь A: `tokens` увеличился на 3
   - Пользователь B: `tokens` увеличился на 2
   - `referrer_bonus_granted` = true, `referee_bonus_granted` = true

**Критерий успеха:** ✅ Реферальные бонусы начислены после первой оплаты

---

### Тест 3: Покупка подписки

**Цель:** Проверить полный цикл покупки и активации подписки

**Шаги:**
1. Открыть бот → "💰 Купить вопросы" → "📅 Подписки"
2. Выбрать план подписки (например, "Стандарт 500₽/мес")
3. Нажать "💳 Оплатить" → перейти на страницу YooKassa
4. Оплатить тестовой картой:
   - **Номер:** 4111 1111 1111 1111
   - **Срок:** 12/24
   - **CVC:** 123
5. После оплаты YooKassa отправит webhook на `YOOKASSA_WEBHOOK_URL`
6. Проверить логи backend: должно быть `Payment [id] processed successfully`
7. Проверить БД:
   ```sql
   -- Проверить payment
   SELECT id, status, paid, amount_rub FROM payments
   WHERE yookassa_payment_id = '[payment_id]';
   -- status = 'succeeded', paid = true

   -- Проверить user_subscriptions
   SELECT * FROM user_subscriptions
   WHERE user_id = [user_id]
   ORDER BY created_at DESC LIMIT 1;
   -- status = 'active', is_active = true

   -- Проверить users
   SELECT subscription_status, subscription_expires_at, tokens
   FROM users WHERE id = [user_id];
   -- subscription_status = 'active'
   -- subscription_expires_at = NOW() + 30 days
   -- tokens = 999 (или tokens_included из плана)
   ```
8. В боте отправить консультационный вопрос
9. Проверить: токены НЕ уменьшаются (подписка активна)

**Критерий успеха:** ✅ Подписка активирована, токены начислены, консультации работают без расхода

---

### Тест 4: Покупка пакета токенов

**Цель:** Проверить разовую покупку вопросов

**Шаги:**
1. Открыть бот → "💰 Купить вопросы" → "🎁 Разовые пакеты"
2. Выбрать пакет (например, "20 вопросов за 200₽")
3. Оплатить тестовой картой
4. Проверить БД:
   ```sql
   -- Проверить payment
   SELECT payment_type, token_package_id, status, paid
   FROM payments WHERE yookassa_payment_id = '[payment_id]';
   -- payment_type = 'token_package', status = 'succeeded', paid = true

   -- Проверить tokens пользователя
   SELECT tokens FROM users WHERE id = [user_id];
   -- tokens увеличился на 20
   ```
5. Отправить консультационный вопрос → токены уменьшились на 1

**Критерий успеха:** ✅ Токены начислены, расходуются при консультациях

---

### Тест 5: Консультации (основной функционал)

**Цель:** Проверить качество и корректность ответов

**Сценарий 1: Уточнение типа культуры**
```
User: "Как подкормить малину?"
Bot: "Какая у вас малина: летняя (обычная) или ремонтантная?"
User: "Ремонтантная"
Bot: [Детальный ответ с учетом типа малины]
```
- ✅ Бот задал уточняющий вопрос
- ✅ Использовал RAG (должны быть упоминания конкретных удобрений из БД)
- ✅ Ответ форматирован (жирный текст, списки)

**Сценарий 2: Конкретная культура сразу**
```
User: "Когда обрезать голубику?"
Bot: [Детальный ответ сразу, без уточнений]
```
- ✅ Бот не задал уточняющих вопросов (культура определена точно)
- ✅ Ответ содержит специфичную информацию для голубики

**Сценарий 3: Неопределенная культура**
```
User: "Как бороться с вредителями?"
Bot: "Уточните, пожалуйста, о какой культуре идет речь?"
User: "Про клубнику"
Bot: "Какая у вас клубника: летняя (июньская) или ремонтантная (НСД)?"
User: "Летняя"
Bot: [Детальный ответ про вредителей клубники летней]
```
- ✅ Многоэтапный диалог работает корректно
- ✅ Контекст сохраняется между сообщениями

**Сценарий 4: Followup-вопрос**
```
User: "Как ухаживать за малиной ремонтантной?"
Bot: [Ответ]
User: "А когда лучше поливать?"
Bot: [Ответ в контексте малины ремонтантной, без повторного запроса культуры]
```
- ✅ Бот помнит культуру из предыдущего вопроса
- ✅ Ответ релевантен той же культуре

**Критерий успеха:** ✅ Все 4 сценария работают корректно

---

### Тест 6: Admin Panel

**Цель:** Проверить мониторинг и управление через веб-интерфейс

**Шаги:**
1. Открыть Admin Panel: `http://localhost:5174`
2. **Live Feed:**
   - Отправить консультационный вопрос от бота
   - Проверить: консультация появилась в Live Feed в реальном времени (SSE)
   - Кликнуть на консультацию → открылась детальная view
3. **CRM:**
   - Перейти в CRM → выбрать клиента
   - Проверить:
     - Отображается имя, username, дата регистрации
     - Вкладка "Платежи" показывает транзакции
     - Activity Feed показывает события (консультации, платежи)
4. **Payments:**
   - Перейти в раздел "Payments"
   - Проверить: список всех платежей, фильтры по статусу
5. **RAG Documents:**
   - Перейти в "Документы"
   - Загрузить тестовый PDF → проверить chunking
   - Проверить: embeddings сгенерированы, chunks отображаются

**Критерий успеха:** ✅ Все разделы Admin Panel работают, SSE обновления приходят

---

## 🚨 Критичные моменты перед продакшн-запуском

### 1. ⚠️ Ngrok URL нестабилен

**Проблема:** URL меняется при каждом перезапуске ngrok

**Решения:**
- **Краткосрочное (для тестирования):**
  - Использовать ngrok paid plan ($8/мес) → постоянный subdomain
  - Альтернативы: localtunnel, serveo.net, localhost.run

- **Долгосрочное (для production):**
  - Развернуть backend на VPS с белым IP и доменом
  - Настроить SSL сертификат (Let's Encrypt)
  - Обновить `YOOKASSA_WEBHOOK_URL` на постоянный адрес
  - Обновить webhook в YooKassa dashboard

**Риск:** Если URL изменится → webhook перестанет работать → платежи не будут обрабатываться

---

### 2. ⚠️ Тестовый режим YooKassa включен

**Текущая конфигурация:**
```bash
YOOKASSA_TEST_MODE=true
YOOKASSA_SECRET_KEY=test_MlN3GUKbwI0IpyUM9hKFt3_tzKi2OOUSJKQQz6KgXBA
```

**Перед production-запуском:**
1. Получить production secret_key от YooKassa
2. Изменить `.env`:
   ```bash
   YOOKASSA_TEST_MODE=false
   YOOKASSA_SECRET_KEY=live_XXXXXXXXXXXXXXXXXX
   ```
3. Протестировать реальный платеж с минимальной суммой (50₽)
4. Проверить webhook обработку с реальными данными

**Риск:** В тестовом режиме реальные деньги НЕ принимаются

---

### 3. ❌ Реферальная система не завершена

**Отсутствует:**
- Обработка deeplink `/start ref_CODE`
- Отображение реферального кода в профиле
- Начисление бонусов после первой оплаты
- Интеграция с Admin Panel CRM

**Оценка:** 3-4 часа доработки (см. раздел "Реферальная программа")

**Риск:** Если запустить без доработки → пользователи не смогут пригласить друзей

---

### 4. ⚠️ Отсутствует система триальных вопросов

**Текущее состояние:**
- Миграция `schema_44_trial_questions.sql` НЕ применена
- Функция начисления пробных вопросов НЕ реализована

**Необходимо:**
1. Применить миграцию `schema_44_trial_questions.sql`
2. В `src/handlers/menu/start.py` добавить:
   ```python
   # После создания пользователя
   if not user.get("trial_questions_granted"):
       trial_count = await settings_repo.get_setting("trial_questions_count", "3")
       await tokens_repo.add_tokens(
           user_id=user["id"],
           amount=int(trial_count),
           operation_type="trial_grant",
           description="Пробные вопросы для нового пользователя",
       )
       await users_repo.mark_trial_granted(user["id"])
   ```
3. Протестировать с новым пользователем

**Риск:** Без этого новые пользователи НЕ получат пробные вопросы → плохой onboarding

---

### 5. ⚠️ OpenAI API конфигурация

**Текущие модели (.env):**
```bash
OPENAI_MODEL_CONSULTATION=gpt-5-mini        # ⚠️ Некорректное название модели!
OPENAI_MODEL_ARTICLE=gpt-4.1-mini           # ⚠️ Некорректное название модели!
OPENAI_MODEL_CLASSIFICATION=gpt-4.1-mini    # ⚠️ Некорректное название модели!
```

**Проблема:** Эти модели не существуют в OpenAI API! (из session-summary.md)

**Корректные названия моделей (февраль 2026):**
- `gpt-4o` — основная модель GPT-4 Turbo
- `gpt-4o-mini` — облегченная версия
- `gpt-3.5-turbo` — GPT-3.5 Turbo (дешевле)

**Рекомендуемая конфигурация:**
```bash
OPENAI_MODEL_CONSULTATION=gpt-4o          # Качество важнее для консультаций
OPENAI_MODEL_ARTICLE=gpt-4o              # Качество статей
OPENAI_MODEL_CLASSIFICATION=gpt-4o-mini   # Классификация не требует топовой модели
OPENAI_MODEL_UTILITY=gpt-4o-mini          # Вспомогательные задачи
```

**Риск:** Текущие названия вызовут ошибки API → бот не сможет отвечать

**Действие:** СРОЧНО исправить `.env` перед запуском!

---

### 6. ⚠️ Баланс OpenAI аккаунта

**Из session-summary.md:** OpenAI аккаунт был исчерпан (`insufficient_quota` error)

**Необходимо:**
1. Проверить текущий баланс: https://platform.openai.com/account/billing/overview
2. Пополнить аккаунт (рекомендуется $50-100 для старта)
3. Настроить биллинг-алерты (warning at 80%, critical at 95%)
4. Мониторить расход в первые дни после запуска

**Риск:** Без баланса бот не сможет работать

---

## ✅ Чеклист перед запуском

### Критичные задачи (MUST HAVE)

- [ ] **Исправить названия моделей OpenAI в .env**
  - Заменить `gpt-5-mini` → `gpt-4o-mini`
  - Заменить `gpt-4.1-mini` → `gpt-4o` или `gpt-4o-mini`
  - Протестировать консультацию после изменений

- [ ] **Пополнить баланс OpenAI**
  - Проверить текущий баланс
  - Пополнить минимум на $50
  - Настроить биллинг-алерты

- [ ] **Применить все миграции БД**
  ```bash
  psql -h localhost -U bot_user -d garden_bot -f db/schema_37_documents_full_text.sql
  psql -h localhost -U bot_user -d garden_bot -f db/schema_38_embedding_nullable.sql
  psql -h localhost -U bot_user -d garden_bot -f db/schema_39_admin_settings.sql
  psql -h localhost -U bot_user -d garden_bot -f db/schema_40_culture_rules_split.sql
  psql -h localhost -U bot_user -d garden_bot -f db/schema_41_category_prompts_migration.sql
  psql -h localhost -U bot_user -d garden_bot -f db/schema_42_llm_settings.sql
  psql -h localhost -U bot_user -d garden_bot -f db/schema_44_trial_questions.sql
  psql -h localhost -U bot_user -d garden_bot -f db/schema_45_pricing_update.sql
  psql -h localhost -U bot_user -d garden_bot -f db/schema_46_referrals.sql
  ```

- [ ] **Реализовать триальные вопросы**
  - Добавить код в `src/handlers/menu/start.py`
  - Протестировать с новым пользователем
  - Проверить начисление tokens

- [ ] **Доработать реферальную программу** (3-4 часа)
  - [ ] `start.py`: обработка deeplink `ref_CODE`
  - [ ] `profile.py`: отображение реферального кода
  - [ ] `payment_service.py`: начисление бонусов после первой оплаты
  - [ ] Протестировать полный цикл

- [ ] **Настроить webhook для production**
  - Опция 1: Ngrok paid plan ($8/мес)
  - Опция 2: Развернуть на VPS с доменом
  - Обновить `YOOKASSA_WEBHOOK_URL` в .env
  - Обновить webhook в YooKassa dashboard

- [ ] **Переключить YooKassa в production режим**
  - Получить production secret_key
  - Изменить `YOOKASSA_TEST_MODE=false`
  - Протестировать реальный платеж (50₽)

### Важные задачи (SHOULD HAVE)

- [ ] **Протестировать все сценарии** (см. раздел "План тестирования")
  - [ ] Тест 1: Триальные вопросы
  - [ ] Тест 2: Реферальная программа
  - [ ] Тест 3: Покупка подписки
  - [ ] Тест 4: Покупка токенов
  - [ ] Тест 5: Консультации
  - [ ] Тест 6: Admin Panel

- [ ] **Добавить мониторинг и алерты**
  - [ ] Настроить логирование критичных ошибок
  - [ ] Алерты на Telegram при ошибках платежей
  - [ ] Мониторинг расхода OpenAI API
  - [ ] Мониторинг uptime backend

- [ ] **Подготовить пользовательскую документацию**
  - FAQ для пользователей
  - Инструкция по покупке подписки/токенов
  - Описание реферальной программы

- [ ] **Backup стратегия**
  - Настроить автобэкапы БД (ежедневно)
  - Проверить восстановление из бэкапа
  - Хранить бэкапы минимум 7 дней

### Желательные задачи (NICE TO HAVE)

- [ ] **Улучшить onboarding**
  - Приветственное сообщение с примерами вопросов
  - Интерактивный тур по функциям бота
  - Примеры консультаций

- [ ] **Добавить аналитику**
  - Метрики конверсии (регистрация → первая покупка)
  - Популярные категории вопросов
  - Retention rate пользователей

- [ ] **CI/CD pipeline**
  - Автодеплой при push в main
  - Автоматические тесты
  - Staging окружение

---

## 📊 Оценка времени на доработку

**Минимально необходимо (для первого запуска):**
- Исправить .env (модели OpenAI): **10 минут**
- Применить миграции БД: **15 минут**
- Реализовать триальные вопросы: **1 час**
- Доработать реферальную программу: **3-4 часа**
- Настроить webhook (ngrok paid): **30 минут**
- Тестирование всех сценариев: **2-3 часа**

**Итого:** 7-9 часов работы

**Рекомендуемое (для стабильного запуска):**
- Развернуть на VPS: **2-4 часа** (вместо ngrok)
- Настроить мониторинг: **2 часа**
- Подготовить документацию: **1-2 часа**
- Backup стратегия: **1 час**

**Итого с рекомендациями:** 13-18 часов

---

## 🎯 Рекомендации по запуску

### Вариант A: Минимальный запуск (ASAP)

**Что делаем:**
1. Исправляем критичные баги (модели OpenAI, миграции)
2. Добираем триальные вопросы
3. **Отключаем реферальную программу** (убираем из меню до завершения разработки)
4. Запускаем с ngrok paid plan ($8/мес)
5. Ограниченный бета-тест (10-20 пользователей)

**Плюсы:** Быстрый запуск (1-2 дня)
**Минусы:** Нет реферальной программы, нестабильный webhook

---

### Вариант B: Полноценный запуск (рекомендуется)

**Что делаем:**
1. Все критичные задачи из чеклиста
2. Реферальная программа полностью реализована
3. Развертываем на VPS с доменом
4. Настраиваем мониторинг и бэкапы
5. Проводим полное тестирование
6. Публичный запуск

**Плюсы:** Стабильная работа, все функции готовы
**Минусы:** Требуется 1-2 недели подготовки

---

### Вариант C: Поэтапный запуск (оптимальный)

**Этап 1 (неделя 1):**
- Критичные баги + триальные вопросы
- Запуск без реферальной программы
- Ngrok paid plan
- Закрытая бета (приглашения)

**Этап 2 (неделя 2):**
- Доработка реферальной программы
- Развертывание на VPS
- Мониторинг + бэкапы
- Открытая бета

**Этап 3 (неделя 3):**
- Сбор фидбека
- Исправление багов
- Улучшение onboarding
- Официальный публичный запуск

**Плюсы:** Контролируемый запуск, время на доработку
**Минусы:** Растянутый старт

---

## 📝 Итоговые выводы

**Готовность к запуску:** 75%

**Что работает отлично:**
- ✅ Режим консультаций (полностью функционален)
- ✅ Прием платежей (YooKassa интеграция готова)
- ✅ Admin Panel (мониторинг и управление)
- ✅ RAG система (поиск в базе знаний)

**Что требует доработки:**
- ⚠️ Реферальная программа (3-4 часа работы)
- ⚠️ Триальные вопросы (1 час)
- ⚠️ Webhook стабильность (ngrok → VPS)
- ⚠️ Конфигурация OpenAI моделей (критично!)

**Рекомендация:**
Выбрать **Вариант C (поэтапный запуск)** — начать с закрытой беты без реферальной программы, доработать функционал в процессе, затем открыть публичный доступ. Это минимизирует риски и даст время на стабилизацию.

---

**Документ создан:** 2026-02-15
**Автор:** Claude Code
**Версия:** 1.0
