# Руководство по тестированию гибридного потока

## Новая логика топиков (с версии 1.2)

### Как работают топики теперь:

1. **Один открытый топик = одна тема разговора**
   - Когда пользователь задаёт первый вопрос → создаётся новый топик
   - Культура определяется и сохраняется в топике
   - Все последующие сообщения используют ТУ ЖЕ культуру (для уточняющих вопросов)

2. **Новый топик создаётся только при:**
   - Команде `/start` (перезапуск бота)
   - Нажатии кнопки "🔄 Вопрос по новой теме" (после получения ответа)
   - Нажатии кнопки "❌ Закрыть" (возврат в главное меню)

3. **Преимущества**:
   - ✅ Пользователь может задавать уточняющие вопросы по той же культуре
   - ✅ Не нужно каждый раз указывать культуру заново
   - ✅ Явное разделение тем через кнопки

---

## Проблема: RAG запускается при уточняющих вопросах

Если вы видите, что RAG запускается ДО того, как пользователь ответил на уточняющий вопрос, причины могут быть следующими:

### Причина 1: Старые открытые топики в БД

**Проблема**: В таблице `topics` остались открытые топики с культурой от предыдущего тестирования.

**Решение**: Закрыть все открытые топики или очистить таблицу:

```bash
# Вариант 1: Закрыть все открытые топики (рекомендуется)
docker exec -i $(docker ps -q -f name=postgres) psql -U bot_user -d garden_bot << 'EOF'
UPDATE topics SET status = 'closed' WHERE status = 'open';
EOF

# Вариант 2: Полная очистка таблицы (если нужно удалить всю историю)
docker exec -i $(docker ps -q -f name=postgres) psql -U bot_user -d garden_bot << 'EOF'
TRUNCATE TABLE topics CASCADE;
EOF

# Или напрямую через psql:
psql -h localhost -U bot_user -d garden_bot -c "UPDATE topics SET status = 'closed' WHERE status = 'open';"
```

### Причина 2: Бот не перезапущен

**Проблема**: Старый код еще работает в памяти процесса бота.

**Решение**: Перезапустить бота:

```bash
# Остановить бота (Ctrl+C)
# Затем запустить заново:
source venv/bin/activate
python -m src.main
```

### Причина 3: Множественные запросы

**Проблема**: Если отправлять сообщения слишком быстро, они могут обрабатываться параллельно.

**Решение**: Подождать завершения предыдущего запроса перед отправкой следующего.

---

## Правильный поток для тестирования

### Тест 1: Вопрос без указания культуры

**Шаги**:
1. Очистить таблицу topics (см. выше)
2. Перезапустить бота
3. Отправить: **"как ухаживать за клубникой"**

**Ожидаемое поведение**:
```
[CULTURE] Автоматически определена культура: клубника общая
[HYBRID_FLOW] culture='клубника общая'
[HYBRID_FLOW] CASE 2: General culture - asking variety
```

Бот спросит: **"Какая у вас клубника: летняя (июньская) или ремонтантная (НСД)?"**

❌ **НЕ должно быть RAG запроса!**

4. Ответить: **"обычная"**

**Ожидаемое поведение**:
```
[VARIETY_CLARIFICATION] Refined culture: 'клубника общая' -> 'клубника летняя'
[RAG] Начинаем поиск в базе знаний
[RAG] Категория: общая консультация
[RAG] Подкатегория (культура): клубника летняя
```

✅ **Теперь RAG запускается!**

---

### Тест 2: Общий вопрос (общая информация)

**Шаги**:
1. Очистить таблицу topics
2. Перезапустить бота
3. Отправить: **"питание растений"**

**Ожидаемое поведение**:
```
[CULTURE] Автоматически определена культура: общая информация
[HYBRID_FLOW] culture='общая информация'
[HYBRID_FLOW] CASE 1: Vague culture - asking clarification WITHOUT RAG
[RAG] Пропущен (skip_rag=True)
[RAG] Причина: этап уточняющих вопросов
```

Бот спросит: **"Уточните, пожалуйста, о какой культуре идёт речь?"**

❌ **НЕ должно быть RAG запроса!**

4. Ответить: **"малина"**

**Ожидаемое поведение**:
```
[CLARIFICATION_ANSWER] Re-classified culture: 'малина общая'
[CLARIFICATION_ANSWER] General culture, asking variety
```

Бот спросит: **"Какая у вас малина: летняя (обычная) или ремонтантная?"**

❌ **НЕ должно быть RAG запроса!**

5. Ответить: **"летняя"**

**Ожидаемое поведение**:
```
[VARIETY_CLARIFICATION] Refined culture: 'малина общая' -> 'малина летняя'
[RAG] Начинаем поиск в базе знаний
[RAG] Категория: общая консультация
[RAG] Подкатегория (культура): малина летняя
```

✅ **Теперь RAG запускается!**

---

### Тест 3: Конкретная культура сразу

**Шаги**:
1. Очистить таблицу topics
2. Перезапустить бота
3. Отправить: **"питание малины обычной в июле"**

**Ожидаемое поведение**:
```
[CULTURE] Автоматически определена культура: малина летняя
[HYBRID_FLOW] culture='малина летняя'
[HYBRID_FLOW] CASE 3: Specific culture - final answer WITH RAG
[RAG] Начинаем поиск в базе знаний
[RAG] Категория: общая консультация
[RAG] Подкатегория (культура): малина летняя
```

✅ **RAG запускается сразу** (это правильно, т.к. культура конкретна!)

Бот сразу даёт финальный ответ.

---

## Диагностика проблем

### Проблема: RAG запускается при уточняющих вопросах

**Проверьте логи**:

1. Ищите строку `[HYBRID_FLOW] CASE X`:
   - CASE 1 → должно быть `skip_rag=True`
   - CASE 2 → не должно быть RAG вообще
   - CASE 3 → должно быть `skip_rag=False`

2. Ищите строку `[RAG] Пропущен (skip_rag=True)` или `[RAG] Начинаем поиск`:
   - Если RAG запускается в CASE 1 или CASE 2 → **это баг**
   - Если RAG запускается в CASE 3 → **это правильно**

3. Проверьте, что не дублируются запросы:
   - Каждое сообщение должно обрабатываться ОДИН раз
   - Если видите два `[HYBRID_FLOW]` для одного сообщения → **бот запущен дважды или сообщение обработано дважды**

---

## Быстрая проверка через SQL

Проверить, что culture сохранена правильно:

```sql
SELECT id, culture, meta, created_at
FROM topics
ORDER BY created_at DESC
LIMIT 5;
```

Правильный результат после "питание растений":
```
culture: общая информация
```

Правильный результат после "как ухаживать за клубникой":
```
culture: клубника общая
```

---

### Тест 4: Уточняющие вопросы в рамках одного топика (НОВОЕ!)

**Шаги**:
1. Закрыть все открытые топики (см. команду выше)
2. Перезапустить бота
3. Отправить: **"питание клубники"**

**Ожидаемое поведение (первый вопрос)**:
```
[CULTURE] Автоматически определена культура: клубника общая
[HYBRID_FLOW] CASE 2: General culture - asking variety
```
Бот спросит: **"Какая у вас клубника: летняя (июньская) или ремонтантная (НСД)?"**

4. Ответить: **"летняя"**

**Ожидаемое поведение (ответ на уточнение)**:
```
[VARIETY_CLARIFICATION] Refined culture: 'клубника общая' -> 'клубника летняя'
[RAG] Начинаем поиск в базе знаний
```
Бот даёт полный ответ с кнопками внизу.

5. **НЕ нажимать кнопки!** Просто написать следующий вопрос: **"а сколько раз поливать?"**

**Ожидаемое поведение (уточняющий вопрос)**:
```
[CULTURE] Используется сохранённая культура топика: клубника летняя
[HYBRID_FLOW] CASE 3: Specific culture - final answer WITH RAG
[RAG] Начинаем поиск в базе знаний
[RAG] Подкатегория (культура): клубника летняя
```

✅ **Культура НЕ сбрасывается!** Бот использует "клубника летняя" из топика и сразу отвечает с RAG.

6. Нажать кнопку **"🔄 Вопрос по новой теме"**

7. Отправить: **"малина летняя"**

**Ожидаемое поведение (новый топик)**:
```
[CULTURE] Автоматически определена культура: малина летняя
[HYBRID_FLOW] CASE 3: Specific culture - final answer WITH RAG
[RAG] Подкатегория (культура): малина летняя
```

✅ **Создан НОВЫЙ топик!** Старый топик закрыт, новая культура "малина летняя" сохранена в новом топике.

---

## Если проблема не решается

1. Проверьте, что вы используете **самую последнюю версию кода**
2. Перезапустите бота
3. Закройте все открытые топики: `UPDATE topics SET status = 'closed' WHERE status = 'open';`
4. Отправьте логи с префиксами `[HYBRID_FLOW]`, `[CULTURE]`, `[RAG]`
