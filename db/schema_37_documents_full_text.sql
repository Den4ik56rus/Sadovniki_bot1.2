-- =============================================================================
-- Schema 37: RAG v2.5 — Сохранение полного текста документа
-- Добавляет поле full_text для генерации контекста с локальным окном
-- =============================================================================

-- Добавляем поле для хранения полного текста документа
ALTER TABLE documents
ADD COLUMN IF NOT EXISTS full_text TEXT;

-- Комментарий для документирования
COMMENT ON COLUMN documents.full_text IS
'Полный текст документа для генерации контекстов с локальным окном (RAG v2.5).
NULL для старых документов (backward compatibility).
Используется в generate_chunk_context_with_window() для извлечения локального окна ±2000 символов вокруг чанка.';
